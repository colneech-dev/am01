--- /home/colin/src/toolchain-installer/yosys/techlibs/xilinx/synth_xilinx.cc	2026-08-13 20:54:27.416866826 +0100
+++ techlibs/xilinx/synth_xilinx.cc	2026-08-22 16:37:51.497987812 +0100
@@ -133,6 +133,11 @@
 		log("        run 'abc' with '-D 1' option to enable flip-flop retiming.\n");
 		log("        implies -dff.\n");
 		log("\n");
+		log("    -luts <costs>\n");
+		log("        override the LUT area costs passed to abc, e.g.\n");
+		log("        -luts 2:2,3,6:5,6,12 to price a LUT7 nearer what it\n");
+		log("        physically occupies. Default: 2:2,3,6:5,10,20\n");
+		log("\n");
 		log("    -abc9\n");
 		log("        use new ABC9 flow (EXPERIMENTAL)\n");
 		log("\n");
@@ -145,6 +150,7 @@
 	std::string top_opt, edif_file, blif_file, json_file, family;
 	bool flatten, retime, ise, noiopad, noclkbuf, nobram, nolutram, nosrl, nocarry, nowidelut, nodsp, uram;
 	bool abc9, dff;
+	std::string lut_costs_override;
 	bool flatten_before_abc;
 	int widemux;
 	int lut_size;
@@ -170,6 +176,7 @@
 		nodsp = false;
 		uram = false;
 		abc9 = false;
+		lut_costs_override.clear();
 		dff = false;
 		flatten_before_abc = false;
 		widemux = 0;
@@ -264,6 +271,10 @@
 				widemux = atoi(args[++argidx].c_str());
 				continue;
 			}
+			if (args[argidx] == "-luts" && argidx + 1 < args.size()) {
+				lut_costs_override = args[++argidx];
+				continue;
+			}
 			if (args[argidx] == "-abc9") {
 				abc9 = true;
 				continue;
@@ -671,7 +682,9 @@
 					if (nowidelut)
 						abc_opts += " -luts 2:2,3,6:5";
 					else if (widelut_size == 8)
-						abc_opts += " -luts 2:2,3,6:5,10,20";
+						abc_opts += " -luts " + (lut_costs_override.empty()
+									   ? std::string("2:2,3,6:5,10,20")
+									   : lut_costs_override);
 					else
 						abc_opts += " -luts 2:2,3,6:5,10,20,40";
 				}
@@ -683,8 +696,17 @@
 			}
 			run("clean");
 
-			if (help_mode || !abc9)
-				run("techmap -map +/xilinx/ff_map.v", "(only if not '-abc9')");
+			// Unconditional, and idempotent: after the map_ffs invocation on the
+			// -abc9 path there are no $_DFF_/$_SDFFE_ cells left for these rules
+			// to match, so this is a no-op there rather than a second mapping.
+			//
+			// Guarding it on !abc9 was correct for a full run but wrong for a
+			// split one: with `-abc9 -run map_luts:` the map_ffs invocation is
+			// skipped because its stage never executes, and this one is guarded
+			// off because abc9 is set. Neither fires, synthesis reports success,
+			// and $_SDFFE_* cells reach the place-and-route tool, which fails
+			// with an error pointing nowhere near the cause.
+			run("techmap -map +/xilinx/ff_map.v");
 			// This shregmap call infers fixed length shift registers after abc
 			//   has performed any necessary retiming
 			if (!nosrl || help_mode)
